namespace Shared.Thermodynamics.Strategies.Equlibriums
{
    // ========================================================================
    // ENUM DE MODOS
    // ========================================================================
    public enum EquilibriumMode
    {
        None = 0,
        PT = 1,
        P_FV = 2,
        T_FV = 3
    }
}
