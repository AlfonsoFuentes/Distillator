@* LEGACY PAGE - OldHome.razor depended on WorkspaceManager (commented out).
   Replaced by Home.razor with ProjectDiagram. *@

@page "/homeold"
@using Client.Services

@implements IDisposable
@attribute [Authorize]

<p><em>OldHome replaced by the new workspace. Use Home page.</em></p>

@code {
    public void Dispose() { }
}
