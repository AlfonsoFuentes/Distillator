<div class="d-flex flex-column align-center gap-2">
    <div class="equation-wrapper">
        <span class="var">f</span><sub class="sub">i</sub><sup class="sup">V</sup> = 
        <span class="var">y</span><sub class="sub">i</sub> <span class="var">φ</span><sub class="sub">i</sub><sup class="sup">V</sup> <span class="var">P</span>
    </div>
    <span class="text-hint">where <span class="var">φ</span><sub class="sub">i</sub><sup class="sup">V</sup> is calculated via SRK-EOS</span>
    
    <div class="equation-wrapper mt-2">
        <span class="var">P</span> = 
        <div class="fraction-large">
            <div class="num"><span class="var">R</span><span class="var">T</span></div>
            <div class="den"><span class="var">v</span> - <span class="var">b</span></div>
        </div>
        <span class="operator">-</span>
        <div class="fraction-large">
            <div class="num"><span class="var">a</span><span class="var">α</span>(<span class="var">T</span>)</div>
            <div class="den"><span class="var">v</span>(<span class="var">v</span> + <span class="var">b</span>)</div>
        </div>
    </div>
</div>

<style>
    .equation-wrapper {
        font-family: "Times New Roman", serif;
        font-size: 1.4rem;
        color: var(--mud-palette-text-primary);
        user-select: none;
        display: flex;
        align-items: center;
        gap: 0.2rem;
    }

    .var { font-style: italic; }
    .sub { font-size: 0.8rem; vertical-align: sub; }
    .sup { font-size: 0.8rem; vertical-align: super; }
    .operator { margin: 0 0.5rem; }

    .fraction-large {
        display: inline-flex;
        flex-direction: column;
        align-items: center;
        vertical-align: middle;
        font-size: 1.1rem;
        margin: 0 0.5rem;
    }

    .num { border-bottom: 1px solid var(--mud-palette-text-primary); padding: 0 0.5rem; width: 100%; text-align: center; }
    .den { padding: 0 0.5rem; width: 100%; text-align: center; }

    .text-hint { 
        font-size: 0.9rem; 
        color: var(--mud-palette-text-secondary); 
        font-family: var(--mud-typography-default-family); 
    }
</style>