namespace Client.Templates.Units
{
    public enum VariableMode
    {
        Standard, // El de 3 columnas (Label | Value | Unit)
        Compact   // El de 1 celda integrada (Value + Unit Popup)
    }
}
